import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  output: 'export',
  basePath: "/ami2026",
  assetPrefix: '/ami2026/',
};

export default nextConfig;
